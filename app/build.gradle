plugins {
    id 'com.android.application'
}

android {
    namespace 'id.deapp.lite'
    compileSdk 35

    defaultConfig {
        applicationId 'id.deapp.lite'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0.0-lite'
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
        }
    }
}
